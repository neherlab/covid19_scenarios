{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "http://example.com/example.json",
  "type": "array",
  "title": "Severity data",
  "description": "Estimated fraction of cases that progress to severe illness",
  "items": {
    "$id": "#/items",
    "type": "object",
    "title": "Table row",
    "description": "Enumerates the fraction of confirmed/severe/critical/fatal cases for a given demographic",
    "default": {},
    "required": ["id", "ageGroup", "isolated", "confirmed", "severe", "critical", "fatal"],
    "properties": {
      "id": {
        "$id": "#/items/properties/id",
        "type": "number",
        "title": "ID",
        "description": "ID of the row",
        "default": "",
        "examples": [0]
      },
      "ageGroup": {
        "$id": "#/items/properties/ageGroup",
        "type": "string",
        "title": "Age group",
        "description": "Range of ages for severity data",
        "default": "",
        "examples": ["0-9", "80+"]
      },
      "isolated": {
        "$id": "#/items/properties/isolated",
        "type": "number",
        "title": "Fraction of demographic that in under lockdown",
        "description": "Target isolation",
        "default": 0,
        "examples": [0],
        "minimum": 0,
        "maximum": 100
      },
      "confirmed": {
        "$id": "#/items/properties/confirmed",
        "type": "number",
        "title": "Confirmed cases",
        "description": "Fraction of cases confirmed by testing",
        "default": "",
        "examples": [30.0],
        "minimum": 0,
        "maximum": 100
      },
      "severe": {
        "$id": "#/items/properties/severe",
        "type": "number",
        "title": "Severe cases",
        "description": "Fraction of confirmed cases that progress to severe illness",
        "default": "",
        "examples": [30.0],
        "minimum": 0,
        "maximum": 100
      },
      "critical": {
        "$id": "#/items/properties/critical",
        "type": "number",
        "title": "Critical cases",
        "description": "Fraction of severe cases that progress to critical care",
        "default": "",
        "examples": [30.0],
        "minimum": 0,
        "maximum": 100
      },
      "fatal": {
        "$id": "#/items/properties/fatal",
        "type": "number",
        "title": "Fatal cases",
        "description": "Fraction of critical cases that are lethal",
        "examples": [30.0],
        "minimum": 0,
        "maximum": 100
      }
    }
  }
}
