$schema: https://json-schema.org/draft-07/schema

title: CaseCounts

type: array

items:
  $ref: '#/definitions/CaseCountsForOneCountry'

definitions:
  CaseCountsForOneCountry:
    type: object

    required:
      - country
      - empiricalData

    properties:
      country:
        type: string
        minLength: 1

      empiricalData:
        type: array
        items:
          $ref: 'EmpiricalDatum.yml#/definitions/EmpiricalDatum'
