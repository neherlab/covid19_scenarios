$schema: http://json-schema.org/draft-07/schema
$id: DateRange
title: DateRange

type: object

additionalProperties: false

required:
  - begin
  - end

properties:
  begin:
    type: string
    format: date
    formatMaximum:
      $data: '1/end'

  end:
    type: string
    format: date
