$schema: http://json-schema.org/draft-07/schema
$id: AgeDistributionArray
title: AgeDistributionArray

type: object

additionalProperties: false

required:
  - all

properties:
  all:
    type: array
    items:
      $ref: 'AgeDistributionData#'
