$schema: http://json-schema.org/draft-07/schema
$id: CaseCountsArray
title: CaseCountsArray

type: object

additionalProperties: false

required:
  - all

properties:
  all:
    type: array
    items:
      $ref: 'CaseCountsData#'
