$schema: http://json-schema.org/draft-07/schema
$id: ScenarioDataMitigation
title: ScenarioDataMitigation

type: object

additionalProperties: false

required:
  - mitigationIntervals

properties:
  mitigationIntervals:
    type: array
    items:
      $ref: 'MitigationInterval#'
