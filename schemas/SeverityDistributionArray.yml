$schema: http://json-schema.org/draft-07/schema
$id: SeverityDistributionArray
title: SeverityDistributionArray

type: object

additionalProperties: false

required:
  - severityDistrbutions

properties:
  severityDistrbutions:
    type: array
    items:
      $ref: 'SeverityDistributionData#'
