Here you can load your scenario parameters in JSON format into the simulator.

For a concrete example of a Scenario JSON File you may export any of the predefined scenarios using "Export"
functionality in the simulator. For Scenario JSON File format reference, see JSON schema
[Shareable.yml](https://github.com/neherlab/covid19_scenarios/blob/master/schemas/Shareable.yml).
