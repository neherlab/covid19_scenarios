Here you can load your scenario parameters in JSON format into the simulator.

---

For a concrete example of a Scenario JSON File you may export any of the predefined scenarios using "Export"
functionality in the simulator.

For reference, see JSON schema at
[github.com/neherlab/covid19_scenarios/schemas/Shareable.yml](https://github.com/neherlab/covid19_scenarios/blob/master/schemas/Shareable.yml).
